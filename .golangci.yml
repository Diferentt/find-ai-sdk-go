run:
  timeout: 3m

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - errcheck
    - ineffassign
    - gosimple
    - misspell
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - examples
